[pytest]
# Collect tests from unified locations without relocating existing files
addopts = -q
python_files = test_*.py *_test.py
# Explicit test paths
testpaths =
    tests
    backend/tests
    backend/app/tests
# Ensure project root is on PYTHONPATH for import resolution
pythonpath = .
