import React from 'react';
import { Box, Typography } from '@mui/material';

const SwarmDashboard = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="body1">
        This is the Swarm Dashboard component.
      </Typography>
    </Box>
  );
};

export default SwarmDashboard;
